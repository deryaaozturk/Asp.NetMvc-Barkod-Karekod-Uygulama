@model Cart

<div class="">
    @if (Model.Lines.Count() > 0)
    {
<small class="navbar-text">
    <b>Sepetiniz:</b>
    @Model.Lines.Sum(x => x.Quantity) ürün
    @Model.ComputeTotalValue().ToString("c")
</small>}
    <a class="btn btn-sm btn-secondary navbar-btn" asp-page="/Order/Cart"
       asp-route-returnurl="/">
        <i class="fa fa-shopping-cart"></i>
    </a>
</div>
